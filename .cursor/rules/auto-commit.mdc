---
description: 每次修改文件后自动提交到 git
alwaysApply: true
---

# 自动提交规则

在本项目中，**每次对文件做出修改后，都必须自动执行一次 git 提交**，无需用户手动要求。

## 触发时机

每当完成以下任意操作后，立即执行自动提交：

- 使用编辑工具修改已有文件（`edit_existing_file` / `single_find_and_replace`）
- 创建新文件（`create_new_file`）
- 删除或重命名文件

## 提交步骤

1. 先查看改动范围：

   ```bash
   git status --short
   ```

2. 将所有改动加入暂存区：

   ```bash
   git add -A
   ```

3. 生成提交信息并提交。提交信息格式：

   ```
   <type>: <简要中文描述>
   ```

   - `type` 取值：`feat`（新功能）、`fix`（修复）、`style`（样式）、`refactor`（重构）、`docs`（文档）、`chore`（杂项）
   - 描述用一句话概括本次改动，例如：`fix: 恢复被误删的 style.css 响应式样式`

   ```bash
   git commit -m "<type>: <简要中文描述>"
   ```

## 注意事项

- 一次改动只提交一次，不要把多次独立修改合并到同一个提交里。
- 如果 `git status` 显示无改动，则跳过提交，不要创建空提交。
- 提交前不要执行 `git push`，除非用户明确要求。
- 若同时修改了多个相互关联的文件（属于同一次改动），可以放进同一个提交。
- 提交信息请使用中文描述，保持与项目已有提交记录风格一致。